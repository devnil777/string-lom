# 🔗 Конструктор строк

**Визуальный конвейерный редактор текста** — мощный браузерный инструмент для обработки, трансформации и анализа текстовых данных. Работает полностью локально, без отправки данных на сервер.

---

## Описание

**Конструктор строк** — это одностраничное веб-приложение (SPA), которое позволяет строить цепочки обработки текста из готовых блоков-инструментов. Вы задаёте исходный текст, добавляете нужные блоки обработки, и получаете финальный результат в реальном времени.

### Ключевые особенности

- **Обработка в реальном времени** — результат обновляется мгновенно при любом изменении
- **Блочная архитектура** — цепочка из последовательных блоков-инструментов, где выход одного блока становится входом следующего
- **Сохранение цепочек** — именованные цепочки хранятся в localStorage и доступны между сессиями
- **Экспорт / Импорт** — цепочки можно копировать в JSON и переносить между устройствами
- **Drag & Drop** — блоки можно перетаскивать мышью для изменения порядка обработки
- **Тёмная тема** — стильный интерфейс с продуманной цветовой палитрой
- **Полная конфиденциальность** — все вычисления выполняются в браузере, данные никуда не отправляются
- **Адаптивная вёрстка** — корректно работает на экранах разного размера

---

## Быстрый старт

1. Откройте файл ``strings2.html`` в любом современном браузере (Chrome, Firefox, Edge)
2. Введите текст в блок **Исходный текст**
3. Нажмите **+ Добавить блок** и выберите нужный инструмент
4. Результат отобразится в блоке **Финальный результат** внизу страницы

> Никакой установки, сервера или интернета (после первой загрузки) не требуется.

---

## Архитектура

Приложение построено на принципе **конвейера (pipeline)**:

``````
Исходный текст --> Блок #1 (Regex) --> Блок #2 (Sort) --> Финальный результат
``````

- **Исходный блок** — точка входа: текст + выбор разделителя строк
- **Блоки обработки** — произвольное количество, расположенных последовательно; каждый принимает массив строк, обрабатывает его и передаёт дальше
- **Финальный результат** — отображение итогового текста с возможностью копирования и выбора разделителя вывода

---

## Доступные инструменты

Все инструменты разделены на **6 категорий** (всего 16 инструментов):

### Поиск и очистка

| Инструмент | Описание |
|---|---|
| **Поиск и замена (Regex)** | Замена текста по регулярному выражению. Поддержка флагов (g, i, m и др.), групп захвата. |
| **AI Cleaner** | Автоматическая очистка типографики: замена длинных тире на обычные, замена угловых кавычек на обычные, удаление нестандартных Unicode-символов. |

### Удаление и фильтрация

| Инструмент | Описание |
|---|---|
| **Удаление дубликатов** | Оставляет только уникальные строки. Опция Trim для игнорирования пробелов по краям. |
| **Найти дубликаты** | Показывает только повторяющиеся строки с возможностью вывода счётчика повторений. |
| **Фильтр строк** | Фильтрация по условию: содержит, не содержит, начинается с, заканчивается на. |

### Порядок и сравнение

| Инструмент | Описание |
|---|---|
| **Сортировка** | Алфавитная (A-Z / Z-A) и числовая (0-9 / 9-0) сортировка списка. |
| **Сравнение (Diff)** | Сравнение входящего списка со вторым. Режимы: только общие, только различия (A-B), все с пометками. |

### Трансформация текста

| Инструмент | Описание |
|---|---|
| **Регистр** | Преобразование регистра: ВСЕ ЗАГЛАВНЫЕ, все строчные, Начало Предложения, Каждое Слово. |
| **Обрезка (Trim)** | Удаление пробелов с краёв строк: слева, справа или с обеих сторон. |
| **Обёртка (Prefix/Suffix)** | Добавление произвольного текста в начало и/или конец каждой строки. |

### Работа с форматами

| Инструмент | Описание |
|---|---|
| **CSV Парсер** | Разбор CSV-данных по шаблону. Настройка разделителя и пропуск заголовка. |
| **Форматирование JSON** | Красивое форматирование JSON: отступ 2/4 пробела, табуляция, минификация. |
| **Извлечение (JSONPath)** | Извлечение данных из JSON по JSONPath-запросам. Работает в двух режимах: один JSON или построчный парсинг. |
| **Разбить строку (Split)** | Разбиение строк по произвольному разделителю. |

### Сборка и шаблоны

| Инструмент | Описание |
|---|---|
| **Объединить строки (Join)** | Склейка списка строк в одну с настройкой разделителя, последнего разделителя и обёрткой. |
| **Добавить строку** | Вставка произвольной строки в начало или конец списка. |
| **Шаблонизатор (Nunjucks)** | Генерация текста по шаблону Nunjucks. Доступны переменные body (весь текст) и lines (массив строк). |
| **Функция (JS)** | Произвольный JavaScript-код, выполняемый для каждой строки. Аргумент функции — line. |

---

## Управление цепочками

### Боковая панель

В левой боковой панели отображается список **сохранённых цепочек**. Возможности:

- **Создать** — сброс текущей рабочей области и начало новой цепочки
- **Сохранить** — сохранение текущей цепочки с именем в localStorage
- **Загрузить** — клик по имени цепочки в списке загружает её
- **Удалить** — кнопка удаления при наведении на элемент списка
- **Переименовать** — клик по заголовку рабочей области

### Экспорт / Импорт

- **Экспортировать** — копирует JSON-конфигурацию цепочки в буфер обмена
- **Импортировать** — вставка ранее экспортированной JSON-конфигурации

Формат экспорта — массив JSON-объектов с типом блока и параметрами:

``````json
[
  { "type": "source", "params": { "delimiter": "\\n" } },
  { "type": "regex", "params": { "pattern": "\\d+", "replacement": "", "flags": "g" } },
  { "type": "sort", "params": { "order": "asc" } }
]
``````

---

## Настройки входных/выходных данных

### Разделитель исходного текста

Определяет, как исходный текст разбивается на строки-элементы:

- Новая строка — по умолчанию
- Запятая
- Точка с запятой
- Пробел
- Пользовательский разделитель

### Разделитель финального вывода

Определяет, как строки результата собираются обратно в текст:

- Новая строка — по умолчанию
- Запятая
- Точка с запятой
- Пробел
- Пользовательский разделитель

---

## Примеры использования

### Очистка списка email-адресов
1. Вставьте список в исходный блок
2. Добавьте **Обрезка (Trim)** — удалить лишние пробелы
3. Добавьте **Регистр** — все строчные
4. Добавьте **Удаление дубликатов**
5. Добавьте **Сортировка** — A-Z

### Извлечение данных из JSON API
1. Вставьте JSON-ответ в исходный блок
2. Добавьте **Извлечение (JSONPath)** — запрос ``$.data[*].email``
3. Добавьте **Сортировка**
4. Добавьте **Удаление дубликатов**

### Генерация SQL VALUES из CSV
1. Вставьте CSV-данные
2. Добавьте **CSV Парсер** — шаблон ``('\$1', '\$2', '\$3')``
3. Добавьте **Объединить строки (Join)** — разделитель ``,``
4. Добавьте **Обёртка** — префикс ``INSERT INTO table VALUES`` и суффикс ``;``

### Сравнение двух списков
1. Вставьте первый список
2. Добавьте **Сравнение (Diff)** — вставьте второй список в поле Список B
3. Выберите режим: общие, различия или все с пометками

---

## Структура проекта

``````
strings/
├── strings2.html    # Основное приложение (конвейерный конструктор)
├── strings.html     # Предыдущая версия (классический интерфейс)
├── readme.md        # Документация
└── archive/         # Архивные версии
``````

---

## Технологии

- **HTML5 / CSS3 / Vanilla JS** — без фреймворков и сборщиков
- **Font Awesome 6.4** — иконки интерфейса
- **JSONPath** — библиотека для JSONPath-запросов
- **Nunjucks** — шаблонизатор от Mozilla
- **localStorage** — хранение сохранённых цепочек

---

## Совместимость

| Браузер | Поддержка |
|---|---|
| Chrome 90+ | Да |
| Firefox 90+ | Да |
| Edge 90+ | Да |
| Safari 15+ | Да |

---

(c) 2026 Конструктор строк. Сконструировано с помощью ИИ.
"@
[System.IO.File]::WriteAllText("d:\scripts\strings\readme.md", $content, [System.Text.UTF8Encoding]::new($false))
Canceled command
…\scripts\strings > $content = @'
# Конструктор строк

**Визуальный конвейерный редактор текста** - мощный браузерный инструмент для обработки, трансформации и анализа текстовых данных. Работает полностью локально, без отправки данных на сервер.

---

## Описание

**Конструктор строк** - это одностраничное веб-приложение (SPA), которое позволяет строить цепочки обработки текста из готовых блоков-инструментов. Вы задаете исходный текст, добавляете нужные блоки обработки, и получаете финальный результат в реальном времени.

### Ключевые особенности

- **Обработка в реальном времени** - результат обновляется мгновенно при любом изменении
- **Блочная архитектура** - цепочка из последовательных блоков-инструментов, где выход одного блока становится входом следующего
- **Сохранение цепочек** - именованные цепочки хранятся в localStorage и доступны между сессиями
- **Экспорт / Импорт** - цепочки можно копировать в JSON и переносить между устройствами
- **Drag & Drop** - блоки можно перетаскивать мышью для изменения порядка обработки
- **Темная тема** - стильный интерфейс с продуманной цветовой палитрой
- **Полная конфиденциальность** - все вычисления выполняются в браузере, данные никуда не отправляются
- **Адаптивная верстка** - корректно работает на экранах разного размера

---

## Быстрый старт

1. Откройте файл `strings2.html` в любом современном браузере (Chrome, Firefox, Edge)
2. Введите текст в блок **Исходный текст**
3. Нажмите **+ Добавить блок** и выберите нужный инструмент
4. Результат отобразится в блоке **Финальный результат** внизу страницы

> Никакой установки, сервера или интернета (после первой загрузки) не требуется.

---

## Архитектура

Приложение построено на принципе **конвейера (pipeline)**:

```
Исходный текст --> Блок #1 (Regex) --> Блок #2 (Sort) --> Финальный результат
```

- **Исходный блок** - точка входа: текст + выбор разделителя строк
- **Блоки обработки** - произвольное количество, расположенных последовательно; каждый принимает массив строк, обрабатывает его и передает дальше
- **Финальный результат** - отображение итогового текста с возможностью копирования и выбора разделителя вывода

---

## Доступные инструменты

Все инструменты разделены на **6 категорий** (всего 16 инструментов):

### Поиск и очистка

| Инструмент | Описание |
|---|---|
| **Поиск и замена (Regex)** | Замена текста по регулярному выражению. Поддержка флагов (g, i, m и др.), групп захвата ($1, $2). |
| **AI Cleaner** | Автоматическая очистка типографики: замена длинных тире на обычные, замена угловых кавычек на обычные, удаление нестандартных Unicode-символов. |

### Удаление и фильтрация

| Инструмент | Описание |
|---|---|
| **Удаление дубликатов** | Оставляет только уникальные строки. Опция Trim для игнорирования пробелов по краям. |
| **Найти дубликаты** | Показывает только повторяющиеся строки с возможностью вывода счетчика повторений. |
| **Фильтр строк** | Фильтрация по условию: содержит, не содержит, начинается с, заканчивается на. |

### Порядок и сравнение

| Инструмент | Описание |
|---|---|
| **Сортировка** | Алфавитная (A-Z / Z-A) и числовая (0-9 / 9-0) сортировка списка. |
| **Сравнение (Diff)** | Сравнение входящего списка со вторым. Режимы: только общие, только различия (A-B), все с пометками. |

### Трансформация текста

| Инструмент | Описание |
|---|---|
| **Регистр** | Преобразование регистра: ВСЕ ЗАГЛАВНЫЕ, все строчные, Начало Предложения, Каждое Слово. |
| **Обрезка (Trim)** | Удаление пробелов с краев строк: слева, справа или с обеих сторон. |
| **Обертка (Prefix/Suffix)** | Добавление произвольного текста в начало и/или конец каждой строки. |

### Работа с форматами

| Инструмент | Описание |
|---|---|
| **CSV Парсер** | Разбор CSV-данных по шаблону ($1, $2...). Настройка разделителя и пропуск заголовка. |
| **Форматирование JSON** | Красивое форматирование JSON: отступ 2/4 пробела, табуляция, минификация. |
| **Извлечение (JSONPath)** | Извлечение данных из JSON по JSONPath-запросам (напр. $.users[*].name). Работает в двух режимах: один JSON или построчный парсинг. |
| **Разбить строку (Split)** | Разбиение строк по произвольному разделителю. |

### Сборка и шаблоны

| Инструмент | Описание |
|---|---|
| **Объединить строки (Join)** | Склейка списка строк в одну с настройкой разделителя, последнего разделителя и оберткой. |
| **Добавить строку** | Вставка произвольной строки в начало или конец списка. |
| **Шаблонизатор (Nunjucks)** | Генерация текста по шаблону Nunjucks. Доступны переменные body (весь текст) и lines (массив строк). |
| **Функция (JS)** | Произвольный JavaScript-код, выполняемый для каждой строки. Аргумент функции - line. |

---

## Управление цепочками

### Боковая панель

В левой боковой панели отображается список **сохраненных цепочек**. Возможности:

- **Создать** - сброс текущей рабочей области и начало новой цепочки
- **Сохранить** - сохранение текущей цепочки с именем в localStorage
- **Загрузить** - клик по имени цепочки в списке загружает ее
- **Удалить** - кнопка удаления при наведении на элемент списка
- **Переименовать** - клик по заголовку рабочей области

### Экспорт / Импорт

- **Экспортировать** - копирует JSON-конфигурацию цепочки в буфер обмена
- **Импортировать** - вставка ранее экспортированной JSON-конфигурации

Формат экспорта - массив JSON-объектов с типом блока и параметрами:

```json
[
  { "type": "source", "params": { "delimiter": "\\n" } },
  { "type": "regex", "params": { "pattern": "\\d+", "replacement": "", "flags": "g" } },
  { "type": "sort", "params": { "order": "asc" } }
]
```

---

## Настройки входных/выходных данных

### Разделитель исходного текста

Определяет, как исходный текст разбивается на строки-элементы:

- Новая строка - по умолчанию
- Запятая
- Точка с запятой
- Пробел
- Пользовательский разделитель

### Разделитель финального вывода

Определяет, как строки результата собираются обратно в текст:

- Новая строка - по умолчанию
- Запятая
- Точка с запятой
- Пробел
- Пользовательский разделитель

---

## Примеры использования

### Очистка списка email-адресов
1. Вставьте список в исходный блок
2. Добавьте **Обрезка (Trim)** - удалить лишние пробелы
3. Добавьте **Регистр** -> все строчные
4. Добавьте **Удаление дубликатов**
5. Добавьте **Сортировка** -> A-Z

### Извлечение данных из JSON API
1. Вставьте JSON-ответ в исходный блок
2. Добавьте **Извлечение (JSONPath)** -> запрос `$.data[*].email`
3. Добавьте **Сортировка**
4. Добавьте **Удаление дубликатов**

### Генерация SQL VALUES из CSV
1. Вставьте CSV-данные
2. Добавьте **CSV Парсер** -> шаблон `('$1', '$2', '$3')`
3. Добавьте **Объединить строки (Join)** -> разделитель `,`
4. Добавьте **Обертка** -> префикс `INSERT INTO table VALUES ` и суффикс `;`

### Сравнение двух списков
1. Вставьте первый список
2. Добавьте **Сравнение (Diff)** -> вставьте второй список в поле Список B
3. Выберите режим: общие, различия или все с пометками

---

## Структура проекта

```
strings/
  strings2.html    # Основное приложение (конвейерный конструктор)
  strings.html     # Предыдущая версия (классический интерфейс)
  readme.md        # Документация
  archive/         # Архивные версии
```

---

## Технологии

- **HTML5 / CSS3 / Vanilla JS** - без фреймворков и сборщиков
- **Font Awesome 6.4** - иконки интерфейса
- **JSONPath** - библиотека для JSONPath-запросов
- **Nunjucks** - шаблонизатор от Mozilla
- **localStorage** - хранение сохраненных цепочек

---

## Совместимость

| Браузер | Поддержка |
|---|---|
| Chrome 90+ | Да |
| Firefox 90+ | Да |
| Edge 90+ | Да |
| Safari 15+ | Да |
